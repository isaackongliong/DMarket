<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css" rel="stylesheet">
  <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/js/bootstrap.bundle.min.js"></script>
  <title>Booklink</title>
</head>
<body>

    <%- include ('partials/navbar.ejs') %>
    <div class="container text-center">
      <div>
        <h2>Product</h2>
        <p>
          <%= reviews[0].productName %>
        </p>
        <p>
          <img src="/images/<%= reviews[0].productImage %>" width="20%">
        </p>
        <p><b>Product Description:</b><br>
          <%= reviews[0].productDescription %>
        </p>
      </div>
      <div class="text-center"><h2>Reviews</h2></div>
      <br>



      <table class="table table-hover small text-center">
        <thead>
            <tr>                   
              <th width="100">Review</th>             
              <th width="100">Image</th>     
              <th width="100">Rating</th>  
              <th width="100">Reviewed By</th>           
              <th width="50">Edit</th>
              <th width="50">Delete</th>
            </tr>
        </thead>
        <tbody>
          <% for(let i=0; i < reviews.length; i++) { %>
            <tr>
              
              <td><a href="/review/<%= reviews[i].reviewId %>"><%= reviews[i].reviewContent %></a></td>
              <td> <img src = "/images/<%= reviews[i].reviewImage %>" width="80%"></td>              
              <td><%= reviews[i].reviewRating %> stars</td>
              <td><%= reviews[i].username %></td>
              <!-- Edit Link -->
              <td><a href="/editReview/<%= reviews[i].reviewId %>">Edit</a></td>
              <!-- Delete Link -->
              <td><a href="/editReview/<%= reviews[i].reviewId %>" onclick="return confirm('Are you sure you want to delete this review?')">Delete</a></td>
            </tr>
          <% } %>
        </tbody>
      </table>
    </div>
</body>
</html>