<!DOCTYPE html>
<html lang="en">
    <head>
        <meta charset="UTF-8">
        <meta name="viewport" content="width=device-width, initial-scale=1.0">
        <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css" rel="stylesheet">
        <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/js/bootstrap.bundle.min.js"></script>
        <title>Booklink - Add new review</title>
    </head>
<body>

    <%- include ('partials/navbar.ejs') %>
    <div class="container">
        <br>
        <div class="text-center">
            <h2>Add New Review for <%=product.productName%></h2>
            <p><%=product.productDescription%></p>
            <p><img src = "/images/<%= product.productImage %>" width="50%"></p>
            <p>Price: $<%=product.productPrice%></p>
        </div>
        <br>
        <div class="text-center">
            <form action="/addReview/<%=product.productId%>" method="POST" enctype="multipart/form-data">
                <label for="reviewContent">Review:</label><br>
                <textarea id="reviewContent" name="reviewContent" required></textarea> <br>
                <label for="reviewRating">Rating:</label><br>
                <select name="reviewRating" id="reviewRating" required >
                  <% for (let i = 1; i <= 5; i++) { %>
                    <option value="<%= i %>"><%= i %> star</option>
                  <% } %>
                </select>
                <br><br>
                <label for="reviewImage">Image:</label><br>
                <input type="file" id="reviewImage" name="reviewImage" accept="image/*" required><br><br>
                <!-- <label for="reviewedBy">Review By:</label><br>
                <input type="text" id="reviewedBy" name="reviewedBy" value="<%=session.user.username%>" required /> <br> -->
                <button type="submit">Add Review</button>
            </form>
        </div>
    </div>
</body>
</html>
